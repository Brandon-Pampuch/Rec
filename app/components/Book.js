import React from 'react';
import Menu from "./Menu"

const Book = (props) => {
    return (
        <div>
            <h1>Book</h1>
            <Menu />

        </div>
    );
}

export default Book;