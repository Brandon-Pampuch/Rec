import React from 'react';
import Menu from "../Menu"
import Basic from "../../utils/Basic"

const Movie = (props) => {
    return (
        <div>
            <h1>Movie</h1>
            <Menu />
            <Basic />
        </div>
    );
}

export default Movie;